
var songlist = <%= h @artist.html_safe %>

    var songplaying = findSongPlaying()
    setTimeout(function(){
   		updateSongPlaying(songplaying)
 	}, 100);
    

if( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) && screen.width < 600) {
 			var menu = document.getElementById("toggleMenu")
			menu.className = "navbar-toggle collapsed collapse"
}
<% @artist = JSON.parse(@artist) %>
<% if !check_if_touch() %>
   
   <% @eventclick = "ondblclick" %>


<% else %>

	<% @eventclick = "onclick" %>
		
<% end %>	


var main = document.getElementById("main")
	main.innerHTML = ""
	main.innerHTML = "<%= j render 'show' , :partials => {:artist => @artist} %>"
	printArtistResult()
	



